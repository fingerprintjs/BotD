import getPluginsArray from './plugins_array'

describe('Sources', () => {
  describe('pluginsArray', () => {
    it('returns an expected value', () => {
      expect(getPluginsArray()).toBeTrue()
    })
  })
})
